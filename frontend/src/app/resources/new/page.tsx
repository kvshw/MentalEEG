import ResourceForm from '@/components/resources/ResourceForm';

export const metadata = {
    title: 'New Resource | MentalEEG',
    description: 'Create a new mental health resource',
};

export default function NewResourcePage() {
    return <ResourceForm />;
} 
import ResourceList from '@/components/resources/ResourceList';

export const metadata = {
    title: 'Resources | MentalEEG',
    description: 'Browse and manage mental health resources',
};

export default function ResourcesPage() {
    return <ResourceList />;
} 